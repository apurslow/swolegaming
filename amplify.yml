version: 1
frontend:
  phases:
    preBuild:
        commands:
          - cd swolegaming-web
          - npm ci
    build:
        commands:
          - npm run build
  artifacts:
    baseDirectory: ./swolegaming-web/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*